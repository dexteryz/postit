
<p class="lead"> Post, vote, and comment on your favorite websites across the web.</p>

<table class="table table-striped table-hover">
  <thead id="heading">
    <tr>
        <th></th>
        <th>Votes</th>
        <th>Title</th>
        <th>Submitted by</th>
        <th>Comments</th>
    </tr>
  </thead>


  <tbody>
        <% counter = 1 %>
    <% @posts.each do |post| %>
    <tr>
        <td id="count"> <%= "#{counter}." %></td>
        <td> 
            <%= link_to post_votes_path(post), method: 'post' do %> 
            <i class="icon-arrow-up"></i>
            <% end %>
            <br/> 
            <%= post.votes.size %>
            <br/>
            <%= link_to post_votes_path(post) do %>
            <i class="icon-arrow-down"></i>
            <% end %> 
            </td>
        <td> <%= link_to(post.title, fix_url(post.url)) %>
            <br/>
            <%= link_to(post.url, fix_url(post.url)) %>
        </td>
        <td><%=  "submitted by #{display_name(post.user)}" %> </td>
        <td id="comments"> <%= link_to "#{post.comments.size} comments", post_comments_path(post) %> </td>
    </tr>
    <% counter += 1 %>
    <% end %>
  </tbody>
</table>

<br />

